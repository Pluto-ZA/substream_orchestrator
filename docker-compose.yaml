services:
  orchestrator:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      EXIT_ON_STREAM_ERROR: "true"
    ports:
      - "3000:3000"
    restart: unless-stopped
    extra_hosts:
      - 'host.docker.internal:host-gateway'
    networks:
      coolify:
        aliases:
          - wallet_balance_orchestrator
networks:
  coolify:
    external: true
    name: coolify
